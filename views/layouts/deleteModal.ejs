
        <!-- Delete Confirmation Modal -->
        <div id="deleteConfirmationModal"
            class="fixed inset-0 flex items-center justify-center hidden overflow-x-hidden overflow-y-auto z-50 outline-none focus:outline-none">
            <div class="absolute inset-0 bg-gray-800 opacity-75"></div> <!-- Blur effect -->
            <div class="relative w-auto max-w-md mx-auto my-6">
                <!-- Modal content -->
                <div class="bg-white rounded-lg shadow-lg relative flex flex-col w-full p-8">
                    <div class="text-center">
                        <h3 class="text-xl font-semibold mb-2">Are you sure?</h3>
                        <p class="text-gray-600 mb-4">This action cannot be undone. Are you sure you want to delete?</p>
                        <div class="flex justify-center">
                            <button id="confirmDeleteButton"
                                class="bg-red-500 text-white px-4 py-2 rounded mr-2">Delete</button>
                            <button id="cancelDeleteButton"
                                class="bg-gray-300 text-gray-700 px-4 py-2 rounded">Cancel</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>